<%
const sidebarWords = {
    en: { dashLink: "Dashboard", cdrLink: "CDR Analytics", empLink: "Employee Metrics", extLink: "Extension Statistics", opLink: "Live Operator", dongleLink: "GSM Dongles", contactsLink: "Address Book", usersLink: "Users", liveClock: "System Clock", themeLabel: "Theme", langLabel: "Language", settingsTitle: "Settings", rowsLabel: "Rows per page" },
    ar: { dashLink: "لوحة التحكم", cdrLink: "تحليلات المكالمات", empLink: "مقاييس الموظفين", extLink: "إحصائيات التمديد", opLink: "لوحة المراقبة المباشرة", dongleLink: "شرائح GSM", contactsLink: "دفتر العناوين", usersLink: "المستخدمين", liveClock: "ساعة النظام", themeLabel: "المظهر", langLabel: "اللغة", settingsTitle: "الإعدادات", rowsLabel: "عدد الصفوف" },
};
const sbT = sidebarWords[currentLang] || sidebarWords['en'];
const isRtl = currentLang === 'ar';
const allowed = typeof allowedTabs !== 'undefined' ? allowedTabs : [];
const isSuper = typeof isSuperAdmin !== 'undefined' && isSuperAdmin;
function can(tab) { return allowed.includes(tab); }
%>

<!-- Prevent theme/JIT flickering loading screen overlay -->
<div id="theme-loading-overlay" style="position: fixed; inset: 0; background-color: #f9fafb; z-index: 999999; display: flex; align-items: center; justify-content: center; transition: opacity 0.15s ease-out; pointer-events: none;">
    <div style="width: 2.25rem; height: 2.25rem; border: 3px solid rgba(220,38,38,0.1); border-top: 3px solid #dc2626; border-radius: 50%; animation: theme-spin 0.7s linear infinite;"></div>
    <style>
        @keyframes theme-spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
    </style>
</div>
<script>
    (function() {
        const theme = localStorage.getItem('theme') || 'light';
        const overlay = document.getElementById('theme-loading-overlay');
        if (theme === 'light') {
            document.documentElement.classList.add('light-theme');
            if (overlay) overlay.style.backgroundColor = '#f9fafb';
        } else {
            document.documentElement.classList.remove('light-theme');
            if (overlay) overlay.style.backgroundColor = '#050507';
        }
    })();
    
    // Hide the loading overlay when all resources are ready
    window.addEventListener('load', function() {
        setTimeout(function() {
            const overlay = document.getElementById('theme-loading-overlay');
            if (overlay) {
                overlay.style.opacity = '0';
                setTimeout(() => {
                    overlay.remove();
                }, 150);
            }
        }, 200);
    });
</script>

<!-- Google Fonts Import for Plus Jakarta Sans & Cairo -->
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;900&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">

<!-- Global Theme Override Styles -->
<style>
    :root, body {
        --bg-primary: #050507 !important; /* OLED black background */
        --bg-secondary: #0b0b0e !important; /* Vantablack card background */
        --bg-card: #121216 !important; /* Charcoal card surfaces */
        --bg-hover: #22222a !important; /* Zinc hover state */
        --border-primary: #1b1b22 !important; /* Soft dark border */
        --border-accent: #2d2d39 !important; /* Slightly brighter border */
        --text-primary: #f4f4f5 !important; /* High contrast Zinc 50 */
        --text-secondary: #b6bfc7 !important; /* User's silver/grey for subtext/metadata */
        --text-muted: #8a949e !important; /* Compliant muted text */
        --accent-color: #dc2626 !important; /* Tailwind red-600 */
        --accent-hover: #b91c1c !important; /* Tailwind red-700 */
        --bg-selected: rgba(220, 38, 38, 0.08) !important;
        --status-online: #22c55e !important; /* Green for online */
        --status-offline: #8a949e !important; /* Grey for offline */
        --status-active: #dc2626 !important; /* Accent color for active state */
        
        font-family: 'Plus Jakarta Sans', 'Cairo', sans-serif !important;
        font-size: 16px !important;
    }
    .light-theme {
        --bg-primary: #f9fafb !important; /* Tailwind bg-gray-50 */
        --bg-secondary: #ffffff !important; /* Tailwind bg-white */
        --bg-card: #ffffff !important; /* Tailwind bg-white */
        --bg-hover: #f3f4f6 !important; /* Tailwind bg-gray-100 */
        --border-primary: #e5e7eb !important; /* Tailwind border-gray-200 */
        --border-accent: #d1d5db !important; /* Tailwind border-gray-300 */
        --text-primary: #111827 !important; /* Tailwind text-gray-900 */
        --text-secondary: #4b5563 !important; /* Tailwind text-gray-600 */
        --text-muted: #6b7280 !important; /* Tailwind text-gray-500 */
        --accent-color: #dc2626 !important; /* Tailwind red-600 */
        --accent-hover: #b91c1c !important; /* Tailwind red-700 */
        --bg-selected: rgba(220, 38, 38, 0.08) !important;
        --status-online: #10b981 !important; /* Tailwind emerald-500 */
        --status-offline: #6b7280 !important; /* Tailwind gray-500 */
        --status-active: #dc2626 !important;
    }
    
    body::before {
        background: 
            radial-gradient(circle at 10% 20%, rgba(204, 9, 12, 0.04) 0%, transparent 40%),
            radial-gradient(circle at 90% 80%, rgba(255, 255, 255, 0.01) 0%, transparent 45%) !important;
    }
    .light-theme body::before {
        background: 
            radial-gradient(circle at 10% 20%, rgba(123, 0, 2, 0.04) 0%, transparent 40%),
            radial-gradient(circle at 90% 80%, rgba(0, 0, 0, 0.02) 0%, transparent 45%) !important;
    }
    
    /* Fonts and sizes styling */
    body {
        font-family: 'Plus Jakarta Sans', 'Cairo', sans-serif !important;
    }
    
    /* Arabic text overrides */
    [lang="ar"], [dir="rtl"], [dir="rtl"] * {
        font-family: 'Cairo', sans-serif !important;
    }
    
    /* English headings and labels - clean geometric display */
    h1, h2, h3, h4, h5, h6 {
        font-family: 'Plus Jakarta Sans', 'Cairo', sans-serif !important;
        font-weight: 800 !important;
    }
    
    /* Telemetry, numbers, tables, codes, logs, terminal - JetBrains Mono */
    .font-mono, td, th, input, select, textarea, pre, code, .font-numeric, #digitalClock {
        font-family: 'JetBrains Mono', 'Plus Jakarta Sans', monospace !important;
    }

    /* Scaling up font sizes across elements for enhanced visibility */
    h2 { font-size: 1.85rem !important; }
    h3 { font-size: 1.35rem !important; }
    h4 { font-size: 1.15rem !important; }
    
    table th {
        font-size: 13px !important;
        letter-spacing: 0.05em !important;
    }
    table td {
        font-size: 15px !important;
    }
    
    input, select, button, textarea {
        font-size: 15px !important;
    }
    
    label {
        font-size: 13px !important;
    }

    /* Header tabs sizing and weight upgrades */
    .nav-item {
        padding: 0.85rem 1.6rem !important; /* Larger horizontal/vertical padding */
        font-size: 16px !important; /* Larger font size */
        font-weight: 800 !important; /* Bold/Extra bold text */
        letter-spacing: 0.03em !important;
    }
    
    .nav-item svg {
        width: 1.15rem !important; /* Scale up tab icons */
        height: 1.15rem !important;
    }
    
    /* Make the active border line under tabs thicker and prominent */
    .nav-item .absolute {
        height: 3px !important;
        border-radius: 9999px !important;
        bottom: 0px !important;
    }
    
    .light-theme .text-white:not(button):not([type="submit"]):not(.btn):not(.btn-primary):not(button *):not([class*="bg-red"] *):not([class*="bg-\[var\(--accent-color\)\]"] *) {
        color: #000000 !important;
    }
    .light-theme select, .light-theme input {
        background-color: #ffffff !important;
        color: #000000 !important;
        border-color: #d4d4dd !important;
    }
    .light-theme select:focus, .light-theme input:focus {
        border-color: var(--accent-color) !important;
    }
    .light-theme .text-slate-100 {
        color: #000000 !important;
    }
    .light-theme .text-zinc-300 {
        color: #1a202c !important;
    }
    .light-theme .text-zinc-400 {
        color: #4a5568 !important;
    }
    .light-theme .text-zinc-500 {
        color: #4a5568 !important;
    }
    .light-theme .text-slate-400 {
        color: #4a5568 !important;
    }
    .light-theme .nav-item {
        color: #4a5568 !important;
    }
    .light-theme .nav-item:hover {
        color: var(--accent-color) !important;
    }
    .light-theme .ext-card.active {
        background-color: #eef1f4 !important;
    }
    .light-theme .ext-card:hover {
        background-color: #f0f2f5 !important;
    }
    .light-theme #switchboardGrid .panel-surface {
        background-color: #ffffff !important;
        border-color: #d4d4dd !important;
    }
    .light-theme #switchboardGrid .bg-gradient-to-br {
        background-image: linear-gradient(to bottom right, #ffffff, rgba(123, 0, 2, 0.05)) !important;
    }
    
    /* White mode high-contrast text overrides */
    .light-theme .text-[var(--text-secondary)] { color: #1a202c !important; }
    .light-theme .text-[var(--text-muted)] { color: #4a5568 !important; }
    .light-theme tr { color: #000000 !important; }
    .light-theme td { color: #000000 !important; }
    .light-theme th { color: #1a202c !important; }
    .light-theme label { color: #1a202c !important; }

    /* Buttons, badges, and accent backgrounds should always keep white text, even in light theme */
    .light-theme button,
    .light-theme button *,
    .light-theme [type="submit"],
    .light-theme [type="submit"] *,
    .light-theme .text-white.bg-\[var\(--accent-color\)\],
    .light-theme .text-white.bg-\[var\(--accent-hover\)\],
    .light-theme button.text-white,
    .light-theme .text-white.btn,
    .light-theme .text-white.btn-primary,
    .light-theme [class*="bg-red"] .text-white,
    .light-theme .bg-\[var\(--accent-color\)\] .text-white,
    .light-theme .bg-\[var\(--accent-hover\)\] .text-white,
    .light-theme .btn,
    .light-theme .btn *,
    .light-theme .bg-[var(--accent-color)],
    .light-theme .bg-[var(--accent-color)] *,
    .light-theme .bg-[var(--accent-hover)],
    .light-theme .bg-[var(--accent-hover)] *,
    .light-theme .btn-primary,
    .light-theme .btn-primary * {
        color: #ffffff !important;
    }

    /* Fix dark borders in light theme globally */
    .light-theme .border-\[\#27272a\],
    .light-theme .border-b-\[\#27272a\],
    .light-theme .border-t-\[\#27272a\],
    .light-theme .border-r-\[\#27272a\],
    .light-theme .border-l-\[\#27272a\],
    .light-theme .border-\[\#27272a\/\.5\],
    .light-theme .border-\[\#27272a\/\.60\],
    .light-theme .border-\[\#27272a\/\.30\],
    .light-theme .border-b-\[\#27272a\/\.60\],
    .light-theme .border-t-\[\#27272a\/\.60\],
    .light-theme [class*="border-[#27272a]"] {
        border-color: #cbd5e1 !important;
    }

    /* Universal rounded-lg border radius */
    .panel-surface, 
    .card-stat, 
    .ext-card, 
    button, 
    input, 
    select, 
    textarea {
        border-radius: 0.5rem !important; /* 8px rounded-lg */
    }

    /* Remove heavy gray backgrounds from lists in light mode */
    .light-theme .bg-black\/30,
    .light-theme .bg-black\/25,
    .light-theme .bg-black\/20,
    .light-theme .bg-black\/15,
    .light-theme .bg-black\/10,
    .light-theme .bg-black\/40 {
        background-color: transparent !important;
    }

    /* Table headers global formatting for light mode */
    .light-theme thead tr {
        background-color: #f9fafb !important; /* bg-gray-50 */
        border-bottom: 1px solid #e5e7eb !important; /* border-gray-200 */
    }
    .light-theme th {
        color: #4b5563 !important; /* text-gray-600 */
        font-size: 0.75rem !important; /* text-xs */
        font-weight: 600 !important; /* font-semibold */
        padding: 0.75rem 1rem !important; /* py-3 px-4 */
        text-transform: uppercase !important; /* uppercase */
        letter-spacing: 0.05em !important; /* tracking-wider */
    }
    .light-theme tbody tr {
        border-bottom: 1px solid #f3f4f6 !important; /* border-gray-100 */
    }
    .light-theme tbody tr:hover {
        background-color: #f9fafb !important; /* hover:bg-gray-50 */
    }

    /* Light mode override for soft status badges */
    .light-theme .bg-green-900\/20 {
        background-color: #d1fae5 !important; /* bg-green-100 */
        color: #065f46 !important; /* text-green-800 */
        border-color: #a7f3d0 !important; /* border-green-200 */
    }
    .light-theme .bg-red-900\/20 {
        background-color: #fee2e2 !important; /* bg-red-100 */
        color: #991b1b !important; /* text-red-800 */
        border-color: #fca5a5 !important; /* border-red-200 */
    }
    .light-theme .bg-zinc-800\/50 {
        background-color: #f3f4f6 !important; /* bg-gray-100 */
        color: #1f2937 !important; /* text-gray-800 */
        border-color: #e5e7eb !important; /* border-gray-200 */
    }
    .light-theme .bg-amber-900\/20 {
        background-color: #fef3c7 !important; /* bg-amber-100 */
        color: #92400e !important; /* text-amber-800 */
        border-color: #fde68a !important; /* border-amber-200 */
    }

    /* Secondary actions styling overrides in light mode */
    .light-theme button[onclick*="reloadDongle"],
    .light-theme button[onclick*="resetUsbPort"],
    .light-theme button[id*="resetBtn-"],
    .light-theme button[onclick*="global-refresh-btn"],
    .light-theme button#global-refresh-btn {
        border-color: #d1d5db !important; /* border-gray-300 */
        background-color: transparent !important; /* bg-transparent */
        color: #374151 !important; /* text-gray-700 */
    }
    .light-theme button[onclick*="reloadDongle"]:hover,
    .light-theme button[onclick*="resetUsbPort"]:hover,
    .light-theme button[id*="resetBtn-"]:hover,
    .light-theme button[onclick*="global-refresh-btn"]:hover,
    .light-theme button#global-refresh-btn:hover {
        background-color: #f9fafb !important; /* hover:bg-gray-50 */
        color: #111827 !important;
    }

    html {
        color-scheme: dark light;
    }
    body {
        min-width: 0;
    }
    .app-topbar {
        gap: 1rem;
        box-shadow: 0 1px 0 rgba(255, 255, 255, 0.04), 0 4px 8px rgba(0, 0, 0, 0.18) !important;
    }
    .app-brand,
    .app-actions {
        min-width: max-content;
    }
    .app-nav {
        flex: 1 1 auto;
        min-width: 0;
        overflow-x: auto;
        overflow-y: hidden;
        justify-content: flex-start;
        scrollbar-width: none;
    }
    .app-nav::-webkit-scrollbar {
        display: none;
    }
    .nav-item {
        min-height: 44px;
        white-space: nowrap;
    }
    .panel-surface.shadow-2xl,
    .panel-surface.shadow-xl,
    .panel-surface.shadow-lg {
        box-shadow: 0 1px 0 rgba(255, 255, 255, 0.035), 0 4px 8px rgba(0, 0, 0, 0.22) !important;
    }
    .card-stat:hover {
        transform: translateY(-1px) !important;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.22) !important;
    }
    input,
    select,
    textarea {
        min-height: 44px;
    }
    input::placeholder,
    textarea::placeholder {
        color: var(--text-muted) !important;
        opacity: 1;
    }
    button:focus-visible,
    a:focus-visible,
    input:focus-visible,
    select:focus-visible,
    textarea:focus-visible,
    [tabindex]:focus-visible {
        outline: 2px solid rgba(255, 59, 48, 0.9);
        outline-offset: 2px;
        box-shadow: 0 0 0 4px rgba(255, 59, 48, 0.16);
    }
    table {
        font-variant-numeric: tabular-nums;
    }
    th,
    td {
        vertical-align: middle;
    }
    thead tr {
        z-index: 5;
    }
    tbody tr {
        transition: background-color 150ms ease, color 150ms ease;
    }
    @media (max-width: 1180px) {
        .app-topbar {
            flex-wrap: wrap;
            align-items: center;
        }
        .app-nav {
            order: 3;
            width: 100%;
            flex-basis: 100%;
            justify-content: flex-start;
            border-top: 1px solid var(--border-primary);
            padding-top: 0.5rem;
        }
    }
    @media (max-width: 900px) {
        body > .flex-1 {
            padding: 1rem !important;
            min-height: auto !important;
        }
        .flex.gap-6.flex-1.min-h-0 {
            flex-direction: column !important;
            min-height: auto !important;
        }
        .w-80.shrink-0,
        .w-60.shrink-0 {
            width: 100% !important;
        }
        .panel-surface {
            border-radius: 0.875rem !important;
        }
    }
    @media (max-width: 640px) {
        .app-topbar {
            padding: 0.875rem 1rem !important;
        }
        #brandLogo {
            width: 2.5rem !important;
            height: 2.5rem !important;
        }
        .nav-item {
            padding-left: 0.75rem !important;
            padding-right: 0.75rem !important;
        }
        #toastContainer {
            left: 1rem !important;
            right: 1rem !important;
            bottom: 1rem !important;
            min-width: 0 !important;
            max-width: none !important;
        }
        table {
            font-size: 0.8125rem;
        }
    }
    @media (prefers-reduced-motion: reduce) {
        *,
        *::before,
        *::after {
            animation-duration: 0.01ms !important;
            animation-iteration-count: 1 !important;
            scroll-behavior: auto !important;
            transition-duration: 0.01ms !important;
        }
        .animate-ping,
        .animate-pulse,
        .animate-spin {
            animation: none !important;
        }
    }
</style>

<!-- Theme Handler Script -->
<script>
    // Prevent theme flickering
    (function() {
        const theme = localStorage.getItem('theme') || 'light';
        if (theme === 'light') {
            document.documentElement.classList.add('light-theme');
            document.addEventListener('DOMContentLoaded', () => {
                document.body.classList.add('light-theme');
                updateThemeUI(true);
            });
        } else {
            document.addEventListener('DOMContentLoaded', () => {
                updateThemeUI(false);
            });
        }
    })();

    function updateThemeUI(isLight) {
        const thumb = document.getElementById('themeThumb');
        const track = document.getElementById('themeToggleBtn');
        const logo = document.getElementById('brandLogo');
        if (thumb) {
            thumb.classList.toggle('translate-x-5', isLight);
            thumb.classList.toggle('translate-x-0', !isLight);
        }
        if (track) {
            if (isLight) {
                track.style.backgroundColor = '#e4e4e7';
            } else {
                track.style.backgroundColor = 'rgba(204, 9, 12, 0.3)';
            }
        }
        if (logo) {
            logo.src = isLight ? '/logo.png' : '/logo_dark.png';
        }
    }

    function toggleTheme() {
        const isLight = document.body.classList.toggle('light-theme');
        document.documentElement.classList.toggle('light-theme', isLight);
        localStorage.setItem('theme', isLight ? 'light' : 'dark');
        updateThemeUI(isLight);
        
        if (window.pieChart) {
            location.reload();
        }
        if (window.location.pathname === '/ext-stats') {
            if (window.currentExtension) {
                window.loadStats(window.currentExtension);
            } else {
                window.loadOverview();
            }
        }
    }

    // Settings dropdown
    document.addEventListener('DOMContentLoaded', () => {
        updateThemeUI(localStorage.getItem('theme') === 'light');

        const settingsBtn = document.getElementById('settingsBtn');
        const settingsMenu = document.getElementById('settingsMenu');
        if (settingsBtn && settingsMenu) {
            function positionMenu() {
                settingsMenu.style.width = ''; // reset to auto first
                const rect = settingsBtn.getBoundingClientRect();
                const gap = 6;
                let left = rect.right - settingsMenu.offsetWidth;
                if (left < gap) left = gap;
                if (left + settingsMenu.offsetWidth > window.innerWidth - gap) {
                    left = window.innerWidth - settingsMenu.offsetWidth - gap;
                }
                settingsMenu.style.left = left + 'px';
                let top = rect.bottom + gap;
                if (top + settingsMenu.offsetHeight > window.innerHeight - gap) {
                    top = window.innerHeight - settingsMenu.offsetHeight - gap;
                }
                if (top < gap) top = gap;
                settingsMenu.style.top = top + 'px';
            }
            settingsBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                const isOpen = !settingsMenu.classList.contains('visible');
                if (isOpen) positionMenu();
                settingsMenu.classList.toggle('visible', isOpen);
                settingsMenu.classList.toggle('invisible', !isOpen);
                settingsMenu.classList.toggle('opacity-0', !isOpen);
                settingsMenu.classList.toggle('scale-95', !isOpen);
                settingsMenu.classList.toggle('opacity-100', isOpen);
                settingsMenu.classList.toggle('scale-100', isOpen);
                settingsMenu.style.pointerEvents = isOpen ? 'auto' : 'none';
            });
            document.addEventListener('click', () => {
                settingsMenu.classList.remove('visible', 'opacity-100', 'scale-100');
                settingsMenu.classList.add('invisible', 'opacity-0', 'scale-95');
                settingsMenu.style.pointerEvents = 'none';
            });
            settingsMenu.addEventListener('click', (e) => e.stopPropagation());
            window.addEventListener('scroll', () => {
                if (settingsMenu.classList.contains('visible')) positionMenu();
            });
            window.addEventListener('resize', () => {
                if (settingsMenu.classList.contains('visible')) positionMenu();
            });
        }
    });
</script>

<script>
function updateClock() {
    var el = document.getElementById('digitalClock');
    if (!el) return;
    var d = new Date();
    el.textContent = d.getHours().toString().padStart(2,'0') + ':' + d.getMinutes().toString().padStart(2,'0') + ':' + d.getSeconds().toString().padStart(2,'0');
}
document.addEventListener('DOMContentLoaded', function() { updateClock(); setInterval(updateClock, 1000); });
</script>

<div class="app-topbar w-full bg-[var(--bg-secondary)] border-b border-[var(--border-primary)] flex flex-row items-center justify-between px-8 py-4 shadow-lg shrink-0 sticky top-0 z-50 <%= isRtl ? 'flex-row-reverse' : '' %>">
    <!-- Brand Logo and Title -->
    <div class="app-brand flex items-center gap-3">
        <img id="brandLogo" src="/logo_dark.png" alt="Sokrat VOIP" class="w-14 h-14 object-contain rounded-lg">
        <div>
            <h1 class="text-base font-black text-[var(--text-primary)] tracking-wider font-vibrant-display leading-none">
                Sokrat <span class="text-[var(--accent-color)]">VOIP</span>
            </h1>
        </div>
    </div>

    <!-- Center Navigation Links -->
    <nav class="app-nav flex items-center gap-1 md:gap-2">
        <a href="/?lang=<%= currentLang %>" 
           class="nav-item flex items-center px-4 py-2.5 rounded-lg transition-all duration-150 text-sm font-semibold relative
           <%= currentPage === '/' ? 'bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] shadow-sm' : 'text-[var(--text-secondary)] hover:bg-[var(--bg-hover)]/50 hover:text-[var(--text-primary)]' %>">
            <svg class="w-4 h-4 me-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
            <span class="hidden sm:inline"><%= sbT.dashLink %></span>
            <% if (currentPage === '/') { %>
                <div class="absolute bottom-0 left-4 right-4 h-[2px] bg-[var(--accent-color)] rounded-t"></div>
            <% } %>
        </a>

        <% if (can('cdr')) { %>
        <a href="/cdr?lang=<%= currentLang %>" 
           class="nav-item flex items-center px-4 py-2.5 rounded-lg transition-all duration-150 text-sm font-semibold relative
           <%= currentPage === '/cdr' ? 'bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] shadow-sm' : 'text-[var(--text-secondary)] hover:bg-[var(--bg-hover)]/50 hover:text-[var(--text-primary)]' %>">
            <svg class="w-4 h-4 me-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
            <span class="hidden sm:inline"><%= sbT.cdrLink %></span>
            <% if (currentPage === '/cdr') { %>
                <div class="absolute bottom-0 left-4 right-4 h-[2px] bg-[var(--accent-color)] rounded-t"></div>
            <% } %>
        </a>
        <% } %>

        <% if (can('ext-stats')) { %>
        <a href="/ext-stats?lang=<%= currentLang %>" 
           class="nav-item flex items-center px-4 py-2.5 rounded-lg transition-all duration-150 text-sm font-semibold relative
           <%= currentPage === '/ext-stats' ? 'bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] shadow-sm' : 'text-[var(--text-secondary)] hover:bg-[var(--bg-hover)]/50 hover:text-[var(--text-primary)]' %>">
            <svg class="w-4 h-4 me-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
            <span class="hidden sm:inline"><%= sbT.extLink %></span>
            <% if (currentPage === '/ext-stats') { %>
                <div class="absolute bottom-0 left-4 right-4 h-[2px] bg-[var(--accent-color)] rounded-t"></div>
            <% } %>
        </a>
        <% } %>

        <% if (can('operator')) { %>
        <a href="/operator?lang=<%= currentLang %>" 
           class="nav-item flex items-center px-4 py-2.5 rounded-lg transition-all duration-150 text-sm font-semibold relative
           <%= currentPage === '/operator' ? 'bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] shadow-sm' : 'text-[var(--text-secondary)] hover:bg-[var(--bg-hover)]/50 hover:text-[var(--text-primary)]' %>">
            <div class="relative flex items-center justify-center me-2">
                <span class="animate-ping absolute inline-flex h-2 w-2 rounded-full bg-[var(--accent-color)] opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2 w-2 bg-[var(--accent-color)]"></span>
            </div>
            <span class="hidden sm:inline"><%= sbT.opLink %></span>
            <% if (currentPage === '/operator') { %>
                <div class="absolute bottom-0 left-4 right-4 h-[2px] bg-[var(--accent-color)] rounded-t"></div>
            <% } %>
        </a>
        <% } %>
        
        <% if (can('gsm-dongles')) { %>
        <a href="/gsm-dongles?lang=<%= currentLang %>" 
           class="nav-item flex items-center px-4 py-2.5 rounded-lg transition-all duration-150 text-sm font-semibold relative
           <%= currentPage === '/gsm-dongles' ? 'bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] shadow-sm' : 'text-[var(--text-secondary)] hover:bg-[var(--bg-hover)]/50 hover:text-[var(--text-primary)]' %>">
            <svg class="w-4 h-4 me-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="5" y="2" width="14" height="20" rx="2" ry="2"></rect>
                <line x1="12" y1="18" x2="12.01" y2="18"></line>
            </svg>
            <span class="hidden sm:inline"><%= sbT.dongleLink %></span>
            <% if (currentPage === '/gsm-dongles') { %>
                <div class="absolute bottom-0 left-4 right-4 h-[2px] bg-[var(--accent-color)] rounded-t"></div>
            <% } %>
        </a>
        <% } %>

        <a href="/contacts?lang=<%= currentLang %>" 
           class="nav-item flex items-center px-4 py-2.5 rounded-lg transition-all duration-150 text-sm font-semibold relative
           <%= currentPage === '/contacts' ? 'bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] shadow-sm' : 'text-[var(--text-secondary)] hover:bg-[var(--bg-hover)]/50 hover:text-[var(--text-primary)]' %>">
            <svg class="w-4 h-4 me-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                <circle cx="12" cy="10" r="3"/>
                <path d="M8 16c0-2 4-3 4-3s4 1 4 3"/>
            </svg>
            <span class="hidden sm:inline"><%= sbT.contactsLink %></span>
            <% if (currentPage === '/contacts') { %>
                <div class="absolute bottom-0 left-4 right-4 h-[2px] bg-[var(--accent-color)] rounded-t"></div>
            <% } %>
        </a>

        <% if (isSuper) { %>
        <a href="/users?lang=<%= currentLang %>" 
           class="nav-item flex items-center px-4 py-2.5 rounded-lg transition-all duration-150 text-sm font-semibold relative
           <%= currentPage === '/users' ? 'bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] shadow-sm' : 'text-[var(--text-secondary)] hover:bg-[var(--bg-hover)]/50 hover:text-[var(--text-primary)]' %>">
            <svg class="w-4 h-4 me-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
            <span class="hidden sm:inline"><%= sbT.usersLink %></span>
            <% if (currentPage === '/users') { %>
                <div class="absolute bottom-0 left-4 right-4 h-[2px] bg-[var(--accent-color)] rounded-t"></div>
            <% } %>
        </a>
        <% } %>

    </nav>

    <!-- Right Side: Clock & Settings -->
    <div class="app-actions flex items-center gap-4">
        <!-- Digital Clock -->
        <div class="text-right hidden md:block">
            <span class="text-[9px] font-black uppercase text-[var(--text-secondary)] tracking-widest block"><%= sbT.liveClock %></span>
            <div id="digitalClock" class="text-sm font-bold text-[var(--accent-color)]" style="font-family:'JetBrains Mono','SF Mono',monospace;letter-spacing:0.08em">00:00:00</div>
        </div>
        
        <div class="h-4 w-[2px] bg-[var(--border-primary)] hidden md:block"></div>

        <!-- Current User -->
        <span class="text-xs font-semibold text-[var(--text-secondary)] hidden md:block"><%= typeof currentUser !== 'undefined' ? currentUser : '' %></span>

        <!-- Settings Dropdown -->
        <div class="relative" id="settingsDropdown">
            <button id="settingsBtn" class="bg-[var(--bg-secondary)] hover:bg-[var(--bg-hover)] text-[var(--text-primary)] border border-[var(--border-primary)] rounded-lg p-1.5 flex items-center justify-center cursor-pointer transition shadow-sm h-8 w-8" title="Settings">
                <svg class="w-4 h-4 text-[var(--text-secondary)]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
            </button>

            <!-- Dropdown Menu -->
            <div id="settingsMenu" class="fixed min-w-[14rem] w-auto bg-[var(--bg-secondary)] border border-[var(--border-primary)] rounded-xl shadow-2xl shadow-black/40 opacity-0 invisible scale-95 transition-all duration-150 z-50 overflow-hidden" style="pointer-events:none">
                <!-- Theme Toggle -->
                <div class="flex items-center justify-between px-4 py-3 border-b border-[var(--border-primary)]">
                    <span class="text-xs font-bold text-[var(--text-secondary)] uppercase tracking-wider"><%= sbT.themeLabel %></span>
                    <button id="themeToggleBtn" onclick="toggleTheme()" class="relative w-10 h-5 rounded-full transition-colors duration-200 cursor-pointer bg-[var(--accent-color)]/30" title="Toggle Light/Dark Theme">
                        <span id="themeThumb" class="absolute top-0.5 left-0.5 w-4 h-4 rounded-full bg-[var(--text-primary)] shadow transition-transform duration-200 translate-x-0"></span>
                    </button>
                </div>
                <!-- Rows per Page -->
                <div class="px-4 py-3 border-b border-[var(--border-primary)]">
                    <span class="block text-xs font-bold text-[var(--text-secondary)] uppercase tracking-wider mb-2"><%= sbT.rowsLabel %></span>
                    <div class="flex gap-2">
                        <% [10, 25, 50, 100].forEach(n => { %>
                            <button onclick="localStorage.setItem('cdrPerPage','<%= n %>'); if(window.location.pathname==='/cdr'){window.location.search=window.location.search.replace(/perPage=\d+/,'')+'&perPage=<%= n %>&page=1'}else{this.classList.add('bg-[var(--accent-color)]','text-white','shadow-sm')}" class="flex-1 px-3 py-1.5 rounded-lg text-xs font-black uppercase tracking-wider transition cursor-pointer bg-[var(--bg-card)] text-[var(--text-secondary)] hover:bg-[var(--bg-hover)] hover:text-[var(--text-primary)]"><%= n %></button>
                        <% }) %>
                    </div>
                </div>
                <!-- Language Selector -->
                <div class="px-4 py-3">
                    <span class="block text-xs font-bold text-[var(--text-secondary)] uppercase tracking-wider mb-2"><%= sbT.langLabel %></span>
                    <div class="flex gap-2">
                        <button onclick="location.href=window.location.pathname + '?lang=en'" class="flex-1 px-3 py-1.5 rounded-lg text-xs font-black uppercase tracking-wider transition cursor-pointer <%= currentLang === 'en' ? 'bg-[var(--accent-color)] text-white shadow-sm' : 'bg-[var(--bg-card)] text-[var(--text-secondary)] hover:bg-[var(--bg-hover)] hover:text-[var(--text-primary)]' %>">EN</button>
                        <button onclick="location.href=window.location.pathname + '?lang=ar'" class="flex-1 px-3 py-1.5 rounded-lg text-xs font-black uppercase tracking-wider transition cursor-pointer <%= currentLang === 'ar' ? 'bg-[var(--accent-color)] text-white shadow-sm' : 'bg-[var(--bg-card)] text-[var(--text-secondary)] hover:bg-[var(--bg-hover)] hover:text-[var(--text-primary)]' %>">AR</button>
                    </div>
                </div>
                <div class="px-4 py-2 border-t border-[var(--border-primary)]">
                    <button id="addRecordingBtn" onclick="openRecordingModal()" class="flex items-center gap-2 text-xs font-bold text-[var(--text-secondary)] hover:text-[var(--accent-color)] transition py-1.5 w-full text-start">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" y1="19" x2="12" y2="23"/><line x1="8" y1="23" x2="16" y2="23"/></svg>
                        Add a Recording
                    </button>
                </div>
                <% if (isSuper) { %>
                <div class="px-4 py-2 border-t border-[var(--border-primary)]">
                    <button onclick="openSmtpModal()" class="flex items-center gap-2 text-xs font-bold text-[var(--text-secondary)] hover:text-[var(--accent-color)] transition py-1.5 w-full text-left">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M22 7l-10 7L2 7"/></svg>
                        Password Reset Email
                    </button>
                </div>
                <% } %>
                <div class="px-4 py-2 border-t border-[var(--border-primary)]">
                    <button onclick="openNetworkModal()" class="flex items-center gap-2 text-xs font-bold text-[var(--text-secondary)] hover:text-[var(--accent-color)] transition py-1.5 w-full text-left">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 4h16v16H4z"/><path d="M9 9h6v6H9z"/><path d="M12 3v6M12 15v6M3 12h6M15 12h6"/></svg>
                        Show Network Info
                    </button>
                </div>
                <div class="border-t border-[var(--border-primary)] px-4 py-2">
                    <a href="/logout" class="flex items-center gap-2 text-xs font-bold text-[var(--text-secondary)] hover:text-[var(--accent-color)] transition py-1.5">
                        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
                        Sign Out
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recording Upload Modal -->
<div id="recordingModal" class="fixed inset-0 z-[200] flex items-center justify-center bg-black/60 backdrop-blur-sm opacity-0 invisible transition-all duration-200" style="pointer-events:none">
    <div class="bg-[var(--bg-secondary)] border border-[var(--border-primary)] rounded-2xl shadow-2xl shadow-black/50 w-full max-w-md mx-4 overflow-hidden">
        <div class="flex items-center justify-between px-5 py-4 border-b border-[var(--border-primary)]">
            <h3 class="text-sm font-black uppercase tracking-wider text-[var(--text-primary)]">Add a Recording</h3>
            <button onclick="closeRecordingModal()" class="text-[var(--text-muted)] hover:text-[var(--text-primary)] transition p-1">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
        </div>
        <form id="recordingForm" onsubmit="return submitRecording(event)" class="p-5 space-y-4">
            <div>
                <label class="block text-xs font-bold text-[var(--text-secondary)] uppercase tracking-wider mb-1.5">Recording Name</label>
                <input type="text" id="recordingName" name="name" required placeholder="e.g. welcome_greeting" class="w-full px-3 py-2 rounded-lg text-sm bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] placeholder:text-[var(--text-muted)] focus:outline-none focus:border-[var(--accent-color)] transition">
            </div>
            <div>
                <label class="block text-xs font-bold text-[var(--text-secondary)] uppercase tracking-wider mb-1.5">Audio File</label>
                <div class="relative">
                    <input type="file" id="recordingFile" name="file" accept="audio/*,.mpeg,.mpg" required class="w-full text-xs text-[var(--text-secondary)] file:mr-3 file:py-1.5 file:px-3 file:rounded-lg file:border file:border-[var(--border-primary)] file:bg-[var(--bg-card)] file:text-xs file:font-bold file:text-[var(--text-primary)] hover:file:bg-[var(--bg-hover)] file:cursor-pointer file:transition cursor-pointer">
                </div>
                <p class="text-[10px] text-[var(--text-muted)] mt-1">Accepted: MP3, M4A, WAV, OGG, FLAC, AAC, MPEG (max 50MB)</p>
            </div>
            <!-- Progress Bar -->
            <div id="uploadProgressWrap" class="hidden space-y-1">
                <div class="flex justify-between text-[10px] font-semibold text-[var(--text-muted)]">
                    <span id="uploadStatus">Uploading...</span>
                    <span id="uploadPercent">0%</span>
                </div>
                <div class="w-full h-1.5 bg-[var(--bg-card)] rounded-full overflow-hidden">
                    <div id="uploadProgressBar" class="h-full bg-[var(--accent-color)] rounded-full transition-all duration-300" style="width:0%"></div>
                </div>
            </div>
            <div class="flex justify-end gap-2 pt-2">
                <button type="button" onclick="closeRecordingModal()" class="px-4 py-2 rounded-lg text-xs font-bold text-[var(--text-secondary)] bg-[var(--bg-card)] hover:bg-[var(--bg-hover)] transition cursor-pointer">Cancel</button>
                <button type="submit" id="recordingSubmitBtn" class="px-4 py-2 rounded-lg text-xs font-bold text-white bg-[var(--accent-color)] hover:bg-[#e0352b] transition cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed">
                    Upload
                </button>
            </div>
        </form>
    </div>
</div>

<!-- SMTP Settings Modal -->
<div id="smtpModal" class="fixed inset-0 z-[200] flex items-center justify-center bg-black/60 backdrop-blur-sm opacity-0 invisible transition-all duration-200" style="pointer-events:none">
    <div class="bg-[var(--bg-secondary)] border border-[var(--border-primary)] rounded-2xl shadow-2xl shadow-black/50 w-full max-w-md mx-4 overflow-hidden">
        <div class="flex items-center justify-between px-5 py-4 border-b border-[var(--border-primary)]">
            <h3 class="text-sm font-black uppercase tracking-wider text-[var(--text-primary)]">Password Reset Email</h3>
            <button onclick="closeSmtpModal()" class="text-[var(--text-muted)] hover:text-[var(--text-primary)] transition p-1">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
        </div>
        <form id="smtpSettingsForm" onsubmit="return saveSmtpSettings(event)" class="p-5 space-y-4">
            <div>
                <label class="block text-xs font-bold text-[var(--text-secondary)] uppercase tracking-wider mb-1.5">Email Address</label>
                <input type="email" id="smtpEmail" name="email" required placeholder="smtp@gmail.com" class="w-full px-3 py-2 rounded-lg text-sm bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] placeholder:text-[var(--text-muted)] focus:outline-none focus:border-[var(--accent-color)] transition">
                <p class="text-[10px] text-[var(--text-muted)] mt-1">Gmail or any SMTP email address</p>
            </div>
            <div>
                <label class="block text-xs font-bold text-[var(--text-secondary)] uppercase tracking-wider mb-1.5">App Password</label>
                <input type="password" id="smtpPassword" name="password" placeholder="App Password" class="w-full px-3 py-2 rounded-lg text-sm bg-[var(--bg-card)] text-[var(--text-primary)] border border-[var(--border-primary)] placeholder:text-[var(--text-muted)] focus:outline-none focus:border-[var(--accent-color)] transition">
                <p class="text-[10px] text-[var(--text-muted)] mt-1">Leave blank to keep existing password</p>
            </div>
            <div id="smtpStatusMsg" class="text-xs hidden text-center"></div>
            <div class="flex justify-end gap-2 pt-2">
                <button type="button" onclick="closeSmtpModal()" class="px-4 py-2 rounded-lg text-xs font-bold text-[var(--text-secondary)] bg-[var(--bg-card)] hover:bg-[var(--bg-hover)] transition cursor-pointer">Cancel</button>
                <button type="submit" class="px-4 py-2 rounded-lg text-xs font-bold text-white bg-[var(--accent-color)] hover:bg-[#e0352b] transition cursor-pointer">Save Settings</button>
            </div>
        </form>
    </div>
</div>

<!-- Network Info Modal -->
<div id="networkModal" class="fixed inset-0 z-[200] flex items-center justify-center bg-black/60 backdrop-blur-sm opacity-0 invisible transition-all duration-200" style="pointer-events:none">
    <div class="bg-[var(--bg-secondary)] border border-[var(--border-primary)] rounded-2xl shadow-2xl shadow-black/50 w-full max-w-lg mx-4 overflow-hidden">
        <div class="flex items-center justify-between px-5 py-4 border-b border-[var(--border-primary)]">
            <h3 class="text-sm font-black uppercase tracking-wider text-[var(--text-primary)]">Network Interfaces</h3>
            <button onclick="closeNetworkModal()" class="text-[var(--text-muted)] hover:text-[var(--text-primary)] transition p-1">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
        </div>
        <div class="p-5" id="networkInfoContent">
            <div class="text-center text-[var(--text-muted)] text-xs py-8">
                <svg class="animate-spin inline-block w-5 h-5 mr-2" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10" stroke-dasharray="31.4 31.4" stroke-linecap="round"/></svg>
                Loading network info...
            </div>
        </div>
        <div class="flex justify-end px-5 py-4 border-t border-[var(--border-primary)]">
            <button onclick="closeNetworkModal()" class="px-4 py-2 rounded-lg text-xs font-bold text-[var(--text-secondary)] bg-[var(--bg-card)] hover:bg-[var(--bg-hover)] transition cursor-pointer">Close</button>
        </div>
    </div>
</div>

<!-- Toast Notification -->
<div id="toastContainer" class="fixed bottom-6 right-6 z-[100] flex flex-col gap-2 pointer-events-none" style="min-width:280px;max-width:420px"></div>

<div id="flashData" data-success="<%= typeof success !== 'undefined' ? success : '' %>" data-error="<%= typeof error !== 'undefined' ? error : '' %>" style="display:none"></div>

<style>
.success-msg, .error-msg { display: none !important; }
</style>

<script>
function openRecordingModal() {
    document.getElementById('recordingModal').classList.remove('opacity-0', 'invisible');
    document.getElementById('recordingModal').style.pointerEvents = 'auto';
    document.getElementById('settingsMenu').classList.remove('visible', 'opacity-100', 'scale-100');
    document.getElementById('settingsMenu').classList.add('invisible', 'opacity-0', 'scale-95');
    document.getElementById('settingsMenu').style.pointerEvents = 'none';
}

function closeRecordingModal() {
    document.getElementById('recordingModal').classList.add('opacity-0', 'invisible');
    document.getElementById('recordingModal').style.pointerEvents = 'none';
    document.getElementById('recordingForm').reset();
    document.getElementById('uploadProgressWrap').classList.add('hidden');
    document.getElementById('uploadProgressBar').style.width = '0%';
}

function showToast(msg, type) {
    var toastContainer = document.getElementById('toastContainer');
    if (!toastContainer) return;
    var el = document.createElement('div');
    el.className = 'pointer-events-auto px-5 py-3 rounded-xl text-sm font-semibold shadow-2xl shadow-black/40';
    el.style.transform = 'translateX(120%)';
    el.style.opacity = '0';
    el.style.transition = 'all 0.3s';
    if (type === 'error') {
        el.style.backgroundColor = 'rgba(255,59,48,0.15)';
        el.style.border = '1px solid rgba(255,59,48,0.35)';
        el.style.color = '#ff6b6b';
    } else {
        el.style.backgroundColor = 'rgba(34,197,94,0.15)';
        el.style.border = '1px solid rgba(34,197,94,0.35)';
        el.style.color = '#4ade80';
    }
    el.textContent = msg;
    toastContainer.appendChild(el);
    requestAnimationFrame(function() {
        el.style.transform = 'translateX(0)';
        el.style.opacity = '1';
    });
    setTimeout(function() {
        el.style.transform = 'translateX(120%)';
        el.style.opacity = '0';
        setTimeout(function() { if (el.parentNode) el.parentNode.removeChild(el); }, 300);
    }, 5000);
}

async function submitRecording(event) {
    event.preventDefault();
    var fileInput = document.getElementById('recordingFile');
    var nameInput = document.getElementById('recordingName');
    var submitBtn = document.getElementById('recordingSubmitBtn');
    var progressWrap = document.getElementById('uploadProgressWrap');
    var progressBar = document.getElementById('uploadProgressBar');
    var progressPercent = document.getElementById('uploadPercent');
    var uploadStatus = document.getElementById('uploadStatus');

    if (!fileInput.files.length) { showToast('Please select a file', 'error'); return; }
    if (!nameInput.value.trim()) { showToast('Please enter a recording name', 'error'); return; }

    submitBtn.disabled = true;
    submitBtn.textContent = 'Uploading...';
    progressWrap.classList.remove('hidden');

    var formData = new FormData();
    formData.append('file', fileInput.files[0]);
    formData.append('name', nameInput.value.trim());

    try {
        var xhr = new XMLHttpRequest();
        var result = await new Promise(function(resolve, reject) {
            xhr.upload.addEventListener('progress', function(e) {
                if (e.lengthComputable) {
                    var pct = Math.round((e.loaded / e.total) * 100);
                    progressBar.style.width = pct + '%';
                    progressPercent.textContent = pct + '%';
                }
            });
            xhr.addEventListener('load', function() {
                if (xhr.status >= 200 && xhr.status < 300) {
                    resolve(JSON.parse(xhr.responseText));
                } else {
                    try { reject(JSON.parse(xhr.responseText)); }
                    catch(e) { reject({ error: 'Upload failed (HTTP ' + xhr.status + ')' }); }
                }
            });
            xhr.addEventListener('error', function() { reject({ error: 'Network error' }); });
            xhr.open('POST', '/api/settings/recordings/upload');
            xhr.send(formData);
        });

        progressBar.style.width = '100%';
        uploadStatus.textContent = 'Complete!';
        if (result.success) {
            showToast(result.message, 'success');
            setTimeout(closeRecordingModal, 1000);
        } else {
            showToast(result.error || 'Upload failed', 'error');
        }
    } catch (err) {
        showToast(err.error || 'Upload failed', 'error');
        uploadStatus.textContent = 'Failed';
    } finally {
        submitBtn.disabled = false;
        submitBtn.textContent = 'Upload';
        setTimeout(function() {
            progressWrap.classList.add('hidden');
            progressBar.style.width = '0%';
        }, 3000);
    }
    return false;
}

document.addEventListener('DOMContentLoaded', function() {
    var toastContainer = document.getElementById('toastContainer');
    if (!toastContainer) return;

    var flash = document.getElementById('flashData');
    var msg = flash ? (flash.dataset.success || flash.dataset.error) : '';
    var type = flash && flash.dataset.error ? 'error' : 'success';
    if (!msg) return;

    function showToast(msg, type) {
        var el = document.createElement('div');
        el.className = 'pointer-events-auto px-5 py-3 rounded-xl text-sm font-semibold shadow-2xl shadow-black/40';
        el.style.transform = 'translateX(120%)';
        el.style.opacity = '0';
        el.style.transition = 'all 0.3s';
        if (type === 'error') {
            el.style.backgroundColor = 'rgba(255,59,48,0.15)';
            el.style.border = '1px solid rgba(255,59,48,0.35)';
            el.style.color = '#ff6b6b';
        } else {
            el.style.backgroundColor = 'rgba(34,197,94,0.15)';
            el.style.border = '1px solid rgba(34,197,94,0.35)';
            el.style.color = '#4ade80';
        }
        el.textContent = msg;
        toastContainer.appendChild(el);

        requestAnimationFrame(function() {
            el.style.transform = 'translateX(0)';
            el.style.opacity = '1';
        });

        setTimeout(function() {
            el.style.transform = 'translateX(120%)';
            el.style.opacity = '0';
            setTimeout(function() { if (el.parentNode) el.parentNode.removeChild(el); }, 300);
        }, 5000);
    }

    window.showToast = showToast;
    showToast(msg, type);
});
</script>

<script>

function copyAddr(btn) {
    var span = btn.parentElement.querySelector('span.font-mono');
    if (!span) return;
    var text = span.textContent.trim();
    if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(text).catch(function(){});
    }
    var range = document.createRange();
    range.selectNodeContents(span);
    var sel = window.getSelection();
    sel.removeAllRanges();
    sel.addRange(range);
    var orig = btn.innerHTML;
    btn.innerHTML = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#22c55e" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>';
    setTimeout(function() { btn.innerHTML = orig; }, 1500);
}

function addrRow(label, value) {
    var copySvg = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>';
    return '<div class="flex items-center gap-2 text-xs text-[var(--text-muted)] pl-5 py-1">' +
        '<span class="text-[11px] uppercase tracking-wider font-bold text-[var(--text-muted)]">' + label + ':</span>' +
        '<span class="text-sm font-mono text-[var(--text-secondary)] tracking-tight">' + value + '</span>' +
        '<button onclick="copyAddr(this)" class="ml-auto p-0.5 rounded hover:bg-[var(--bg-hover)] transition cursor-pointer opacity-40 hover:opacity-100" title="Copy ' + label + '">' + copySvg + '</button></div>';
}

function openSmtpModal() {
    document.getElementById('settingsMenu').classList.remove('visible', 'opacity-100', 'scale-100');
    document.getElementById('settingsMenu').classList.add('invisible', 'opacity-0', 'scale-95');
    document.getElementById('settingsMenu').style.pointerEvents = 'none';
    document.getElementById('smtpModal').classList.remove('opacity-0', 'invisible');
    document.getElementById('smtpModal').style.pointerEvents = 'auto';
    loadSmtpSettings();
}

function closeSmtpModal() {
    document.getElementById('smtpModal').classList.add('opacity-0', 'invisible');
    document.getElementById('smtpModal').style.pointerEvents = 'none';
}

async function loadSmtpSettings() {
    try {
        const resp = await fetch('/api/settings/smtp');
        const data = await resp.json();
        if (data.success) {
            if (data.email) {
                document.getElementById('smtpEmail').value = data.email;
            }
            if (data.hasPassword) {
                document.getElementById('smtpPassword').placeholder = '\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022 (Saved)';
                document.getElementById('smtpPassword').value = '';
                document.getElementById('smtpPassword').required = false;
            } else {
                document.getElementById('smtpPassword').placeholder = 'App Password';
                document.getElementById('smtpPassword').required = true;
            }
        }
    } catch (e) {
        console.error('Failed to load SMTP settings:', e);
    }
}

async function saveSmtpSettings(event) {
    event.preventDefault();
    const email = document.getElementById('smtpEmail').value;
    const password = document.getElementById('smtpPassword').value;
    const msgEl = document.getElementById('smtpStatusMsg');
    
    msgEl.classList.remove('hidden');
    msgEl.className = 'text-xs text-zinc-400 text-center';
    msgEl.textContent = 'Saving...';
    
    try {
        const resp = await fetch('/api/settings/smtp', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ email, password })
        });
        const data = await resp.json();
        if (data.success) {
            msgEl.className = 'text-xs text-green-400 text-center';
            msgEl.textContent = 'Saved successfully!';
            document.getElementById('smtpPassword').value = '';
            document.getElementById('smtpPassword').placeholder = '\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022\u2022 (Saved)';
            document.getElementById('smtpPassword').required = false;
            if (typeof window.showToast === 'function') {
                window.showToast('SMTP settings saved', 'success');
            }
            setTimeout(closeSmtpModal, 1200);
        } else {
            msgEl.className = 'text-xs text-red-400 text-center';
            msgEl.textContent = data.error || 'Failed to save settings.';
        }
    } catch (e) {
        msgEl.className = 'text-xs text-red-400 text-center';
        msgEl.textContent = 'Error saving settings.';
    }
}

function openNetworkModal() {
    document.getElementById('networkModal').classList.remove('opacity-0', 'invisible');
    document.getElementById('networkModal').style.pointerEvents = 'auto';
    document.getElementById('settingsMenu').classList.remove('visible', 'opacity-100', 'scale-100');
    document.getElementById('settingsMenu').classList.add('invisible', 'opacity-0', 'scale-95');
    document.getElementById('settingsMenu').style.pointerEvents = 'none';
    loadNetworkInfo();
}

function closeNetworkModal() {
    document.getElementById('networkModal').classList.add('opacity-0', 'invisible');
    document.getElementById('networkModal').style.pointerEvents = 'none';
}

async function loadNetworkInfo() {
    var container = document.getElementById('networkInfoContent');
    try {
        var resp = await fetch('/api/network-info');
        var data = await resp.json();
        if (!data.success) throw new Error(data.error);
        if (!data.interfaces || data.interfaces.length === 0) {
            container.innerHTML = '<div class="text-center text-[var(--text-muted)] text-sm py-8">No network interfaces found</div>';
            return;
        }
        var html = '';
        if (data.gateway) {
            html += '<div class="flex items-center gap-2.5 px-3 py-2.5 mb-3 rounded-lg bg-[var(--bg-card)] border border-[var(--border-primary)]">';
            html += '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="var(--accent-color)" stroke-width="2"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>';
            html += '<span class="text-[11px] font-bold text-[var(--text-muted)] uppercase tracking-wider">Gateway:</span>';
            html += '<span class="text-sm font-mono text-[var(--text-primary)] tracking-tight">' + data.gateway + '</span>';
            html += '<button onclick="copyAddr(this)" class="ml-auto p-0.5 rounded hover:bg-[var(--bg-hover)] transition cursor-pointer opacity-40 hover:opacity-100" title="Copy gateway"><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg></button></div>';
        }
        html += '<div class="space-y-2.5">';
        for (var i = 0; i < data.interfaces.length; i++) {
            var iface = data.interfaces[i];
            html += '<div class="px-3 py-3 rounded-lg bg-[var(--bg-card)] border border-[var(--border-primary)]">';
            html += '<div class="flex items-center justify-between mb-2">';
            html += '<div class="flex items-center gap-2.5">';
            html += '<span class="w-2.5 h-2.5 rounded-full ' + (iface.state === 'up' ? 'bg-green-400' : 'bg-red-400') + '"></span>';
            html += '<span class="text-sm font-bold text-[var(--text-primary)] font-mono">' + iface.name + '</span>';
            html += '<span class="text-[11px] ' + (iface.state === 'up' ? 'text-green-400' : 'text-red-400') + ' uppercase font-bold">' + iface.state + '</span>';
            html += '</div></div>';
            if (iface.mac) html += addrRow('MAC', iface.mac);
            if (iface.ip4) html += addrRow('IPv4', iface.ip4);
            if (iface.ip6) html += addrRow('IPv6', iface.ip6);
            html += '</div>';
        }
        html += '</div>';
        container.innerHTML = html;
    } catch (e) {
        container.innerHTML = '<div class="text-center text-red-400 text-sm py-8">Failed to load network info: ' + e.message + '</div>';
    }
}
</script>



